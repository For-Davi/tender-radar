[project]
name = "radar-dbt"
version = "0.1.0"
description = "Camada analítica (gold) do Radar de Licitações com dbt"
requires-python = ">=3.12,<3.13"
# faixa até a próxima minor: versões novas do dbt às vezes mudam comportamento
dependencies = ["dbt-core>=1.12,<1.13", "dbt-postgres>=1.11,<1.12"]

[tool.uv]
package = false  # projeto de SQL: não há pacote Python para instalar
